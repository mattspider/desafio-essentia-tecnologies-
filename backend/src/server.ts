/**
 * TechX To-Do API — entry point.
 * Express bootstrap and DI container setup will be added in Phase 2.
 */
const port = process.env.PORT ?? '3000';

console.log(`TechX Todo API bootstrap ready (port ${port})`);
