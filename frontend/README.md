# TechX To-Do — Frontend

SPA Angular do projeto. Documentação completa na [raiz do repositório](../README.md).

```bash
npm install
npm start          # http://localhost:4200
npm run build      # dist/techx-todo/browser
```

API padrão: `http://localhost:3000/api` (ver `src/environments/`).
